import UploadedDataClientPage from "./client-page";

export const metadata = { title: "Uploaded Data — Extended Services" };

export default async function UploadedDataPage() {
  return <UploadedDataClientPage />;
}
